spring:
  application:
    name: tribetalk

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

  redis:
    host: ${SPRING_REDIS_HOST:localhost}
    port: 6379

  cache:
    type: redis

  datasource:
    url: jdbc:postgresql://localhost:5432/tribetalk
    driver-class-name: org.postgresql.Driver
    username: admin
    password: admin123

  sql:
    init:
      mode: never

  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update        # or 'none' / 'validate' / 'create' depending on your setup
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

#  h2:
#    console:
#      enabled: false
#      path: /h2-console

  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://admin:admin123@localhost:27017/tribetalknosqldb?authSource=admin}

  aws:
#    accessKey: AKIAXPGTL45WIMP5MKJX
#    secretKey: 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
    region: eu-north-1
    bucket: tribetalk-media-images

  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ${GITHUB_CLIENT_ID:Ov23lizJ8kpXKzpZZi2P}
            client-secret: ${GITHUB_CLIENT_SECRET:58d5de6261dc8b9d2585f513e39cb4c443e63e66}
            redirect-uri: ${GITHUB_REDIRECT_URI:http://k8s-default-tribetal-089de13287-2075252521.eu-north-1.elb.amazonaws.com/login/oauth2/code/github}
            scope:
              - read:user
              - user:email
          google:
            client-id: ${GOOGLE_CLIENT_ID:634703536163-6tqkifb0cg96l302sabbgimf5t4r3qqk.apps.googleusercontent.com}
            client-secret: ${GOOGLE_CLIENT_SECRET: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}
            redirect-uri: ${GOOGLE_REDIRECT_URI:http://localhost:8080/login/oauth2/code/google}
            scope:
              - openid
              - profile
              - email
        provider:
          github:
            authorization-uri: https://github.com/login/oauth/authorize
            token-uri: https://github.com/login/oauth/access_token
            user-info-uri: https://api.github.com/user
            user-name-attribute: login
#existingclientsecret - 0dc9230ac778fe76c4c4645ed1fe685f1dc8d502
#existingclientid     - Ov23liGm8fihan8YBuCU
frontend:
  url: ${FRONTEND_URL:http://localhost:5173}


server:
  port: 8080

logging:
  level:
    root: INFO
    org.springframework.security: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg [traceId=%X{trace_id}] [spanId=%X{span_id}] [requestId=%X{request_id}] [user=%X{user}]%n"

jwt:
  secret: ${JWT_SECRET:change_this_to_a_long_random_secret_with_min_256_bits}
  expirationMillis: 3600000

otel:
  exporter:
    otlp:
      endpoint: ${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4318}
      protocol: http/protobuf
  logs:
    exporter: none
  traces:
    exporter: ${OTEL_TRACES_EXPORTER:none}
  service:
    name: tribetalk

management:
  tracing:
    enabled: ${OTEL_TRACING_ENABLED:false}
    sampling:
      probability: 1.0
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    include: health, info, metrics, prometheus
    prometheus:
      enabled: false
    health:
      show-details: always
  metrics:
    export:
      prometheus:
        enabled: false
    distribution:
      percentiles-histogram:
        http.server.requests: false
    tags:
      application: ${spring.application.name}
  prometheus:
    metrics:
      export:
        enabled: false

  kafka:
    producer:
      bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer


app:
  url: ${APP_URL:http://localhost:5173}
